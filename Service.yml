spring:
  boot:
    admin:
      client:
        url: http://localhost:1111/
  datasource:
   password: PHW#84#jeor
    url: jdbc:mysql://localhost:3306/sbms39
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
   
management:
  endpoints:
    web:
      exposure:
        include: '*'

server:
  port: 9091

